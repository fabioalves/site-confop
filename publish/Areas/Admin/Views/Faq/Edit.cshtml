@model confop.Models.Faq

@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Editar Pergunta</h2>
<p>
    @Html.ActionLink("Voltar para a Lista", "Index")
</p>
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Perguntas Frequentes</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Pergunta)
        </div>
        <div class="editor-field">
            @Html.TextAreaFor(model => model.Pergunta, new { @cols = "50" })
            @Html.ValidationMessageFor(model => model.Pergunta)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Resposta)
        </div>
        <div class="editor-field">
            @Html.TextAreaFor(model => model.Resposta, new { @cols = "50" })
            @Html.ValidationMessageFor(model => model.Resposta)
        </div>

        @Html.HiddenFor(model => Model.SiteId)

        <p>
            <input type="submit" value="Salvar" />
        </p>
    </fieldset>
}

